# repo-with-nested-testdata

Fixture repository used to test that DevArchitect AI excludes a nested
`testdata/` directory when this repository is the scan root, but still
scans that same `testdata/` directory correctly when it is given directly
as the scan root.
